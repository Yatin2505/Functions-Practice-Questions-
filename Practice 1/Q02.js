/* Q2. Create a function named sayHello. It should accept one parameter
called name. Inside the function, print "Hello, " + name + "!" to the console.
Call the function, passing your own name as the argument. */

function sayHello(name) {                // Creating the Function wtih the peremeter

    console.log(`Hello, ${ name} !`);    // printing the string 
    
}
sayHello("Yatin")                       // calling the function with argument 